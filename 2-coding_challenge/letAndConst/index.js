const player = "Abdulquadri"
const opponent = "Olanrewaju"
const game = "amazingFighter"
let points  = 0
let hasWon = false
points += 100
hasWon = true
if (hasWon) {
	console.log(`${player} got ${points} and has won the ${game} game!`)
else {
	console.log(`The winner is ${opponent}! and ${player} lost the game!`)
